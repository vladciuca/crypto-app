import React from "react";
import { ComingSoon } from "components";

export default class Portfolio extends React.Component {
  state = {};
  render() {
    return <ComingSoon />;
    //Title: portfolio value
    // add amount bought for fav coin and date select
    //show added coin with icon name current price and price change
    //add pice chart based on amount
  }
}
